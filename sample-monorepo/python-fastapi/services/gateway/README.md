# gateway

Public HTTP entry point: validates incoming requests and forwards them to the
right internal service. Listens on `GATEWAY_PORT` (default 8000).

Implementation arrives during the course labs; only the path is fixed.
