# payments

Owns payment records and decides whether a refund is approved or rejected.
Listens on `PAYMENTS_PORT` (default 8001).

Implementation arrives during the course labs; only the path is fixed.
