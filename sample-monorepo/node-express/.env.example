# Copy to .env before starting any service (init.sh does this for you).
GATEWAY_PORT=8000
PAYMENTS_PORT=8001
NOTIFICATIONS_PORT=8002

# Fake SMTP endpoint used by the notifications service. No real mail is sent.
FAKE_SMTP_URL=smtp://localhost:1025
