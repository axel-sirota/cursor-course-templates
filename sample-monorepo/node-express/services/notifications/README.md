# notifications

Consumes notification events and records them by appending to
`notifications.log`. Listens on `NOTIFICATIONS_PORT` (default 8002).

Implementation arrives during the course labs; only the path is fixed.
